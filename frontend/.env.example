# Development defaults use Vite's same-origin proxies.
VITE_JAVA_API_BASE=/api
VITE_AGENT_API_BASE=/agent-api

# Optional override for deployments where WebSocket is hosted separately.
# VITE_WS_URL=wss://example.com/ws
