# Fault Injection Tests

覆盖 Parser、Embedding、ES、Reranker、LLM、Java、Trace 和消息设施的超时、崩溃和重复执行。后续增加 Provider 超时/Schema 漂移、MCP 不可信输出、Conversation Store 冲突、Task Store 恢复、重复 Tool Call、预算耗尽、Worker 非法 Artifact 和 Replan 版本冲突。
