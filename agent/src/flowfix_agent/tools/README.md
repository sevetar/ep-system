# Tools

公共 Capability Platform：Registry 注册合同与 Provider，Resolver 选择实现，Policy 计算权限，Gateway 执行校验、预算、超时和 Observation 裁剪。MCP 已包含 Fake 合同替身、真实 Streamable HTTP Client Provider 与只读 Server；远端映射仍必须静态注册，不能通过动态发现扩权。
